System.out.print("Enter your age: ");
            // int userAge = scanner.nextInt();
            // if (userAge < 0) {
            //     throw new IllegalArgumentException("Invalid age. Age cannot be negative.");
            // }
            // System.out.println("Entered age is: " + userAge + " years.");